# Migration: fictitious param for testing the migration pipeline
#
# This spec exists purely to exercise backfill_params.py + rehash_identities.py.
# The param does not correspond to any real TrainingConfig field — it is written
# to MLflow params only and does NOT affect identity_hash (hash-excluded test).
#
# Delete this file and revert the MLflow params after testing.

description: "Fictitious param migration (test only — delete after verifying)"

params:
  test_fictitious_param: "42"   # has no real meaning; migration default is 42
